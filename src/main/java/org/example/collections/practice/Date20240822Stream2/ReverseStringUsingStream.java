package org.example.collections.practice.Date20240822Stream2;

import java.util.Arrays;
import java.util.Collections;
import java.util.stream.Collectors;

public class ReverseStringUsingStream {
    public static void main(String[] args) {
        String str="Hello world";
         // Arrays.stream(str.split("")).sorted(Collections.reverseOrder()).collect(Collectors.joining()).chars.forEach(System.out::println
          //);
    }
}
